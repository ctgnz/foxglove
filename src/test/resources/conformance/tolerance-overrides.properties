# Per-test overrides of ConformanceComparator's default differing-pixel-ratio tolerance (#44).
# testname=ratio, e.g. shapes-rect-01-t=0.05 for a legitimately borderline case. Empty until a real baseline
# run finds one that actually needs it.
